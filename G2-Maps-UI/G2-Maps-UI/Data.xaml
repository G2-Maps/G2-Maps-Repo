<Page
    x:Class="G2_Maps_UI.Data"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:G2_Maps_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#FFE1E1E1">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.3*"/>
        </Grid.RowDefinitions>

        <ScrollBar x:Name="scroll" Width="20" Orientation="Vertical" HorizontalAlignment="Right" Visibility="Visible" IndicatorMode="MouseIndicator" Grid.Row="1"/>

        <Button x:Name="back" Content="X" FontStyle="Italic" FontWeight="SemiBold" HorizontalAlignment="Right" Background="Transparent"/>

        <Frame x:Name="FrameScollbar" Grid.Row="1"/>
    </Grid>
</Page>
